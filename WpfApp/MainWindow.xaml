<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1000">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Button Name="btn_productPage" Content="Produkter" Click="btn_productPage_Click" />
                <Button Name="btn_customerPage" Content="Kunder" Click="btn_customerPage_Click" />
                <Button Name="btn_orderPage" Content="Ordrar" Click="btn_orderPage_Click" />
            </StackPanel>
            
            <Frame Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
            
        </Grid>
    </Grid>
</Window>
